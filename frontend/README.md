
  # Convert HTML to Figma format

  This is a code bundle for Convert HTML to Figma format. The original project is available at https://www.figma.com/design/Om32jHI5L1AtJNAY2PhEdH/Convert-HTML-to-Figma-format.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  